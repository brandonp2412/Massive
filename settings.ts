export default interface Settings {
  minutes: number;
  seconds: number;
  alarm: number;
  vibrate: number;
  predict: number;
  sets: number;
  sound: string;
  notify: number;
  images: number;
}
